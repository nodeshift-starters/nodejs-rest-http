apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nodejs-rest-http-helm.fullname" . }}-test-connection"
  labels:
    {{- include "nodejs-rest-http-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nodejs-rest-http-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
